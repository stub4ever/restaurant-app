<%= link_to 'Back', restaurant_path(@restaurant) %>

<h1>Add review</h1>

<%= form_for([@restaurant, @review]) do |f| %>

  <%= f.label :rating, 'Rate 1-5' %>
  <%= f.text_field :rating %>

  <%= f.label :content, 'Give comments' %>
  <%= f.text_field :content %>

  <%= f.submit %>
<% end %>
